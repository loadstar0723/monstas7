#!/bin/sh
# 🚨 Git Pre-commit Hook - 가짜 데이터 차단

echo "🔍 커밋 전 코드 검증 시작..."

# 프론트엔드 디렉토리로 이동
cd frontend

# 가짜 데이터 검증 실행
node scripts/enforce-no-fake-data.js

if [ $? -ne 0 ]; then
  echo "❌ 커밋 실패! 규칙 위반을 먼저 수정하세요."
  exit 1
fi

echo "✅ 코드 검증 통과!"
exit 0